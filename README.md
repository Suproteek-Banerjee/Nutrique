# Nutrique Meal Kits

Premium React/Vite frontend prototype for Nutrique Meal Kits.

## Local Development

```bash
npm install
npm run dev
```

## Production Build

```bash
npm run build
```

The project is Vercel-ready with `vercel.json` configured for Vite output in `dist`.
